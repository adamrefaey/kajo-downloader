## Summary

<!-- What changed and why (1–3 sentences). -->

## Checklist

- [ ] `pnpm run typecheck` passes
- [ ] `pnpm run lint` passes (Biome)
- [ ] `pnpm run test` or `pnpm run test:coverage` as appropriate for touched code
- [ ] Docs updated if behavior, env vars, or user-facing flows changed
- [ ] Manually tested (describe briefly):

<!-- e.g. macOS dev build, download queue, site sign-in -->
